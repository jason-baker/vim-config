" Allow 'gq' to wrap TeX at 100 columns.
set tw=100

" But don't automatically wrap the other source code.
set fo-=t

" We want to auto-indent this stuff usually.
set ai

" And it's helpful for hanging indents to have this.
set fo+=2
